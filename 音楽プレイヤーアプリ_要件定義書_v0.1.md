# 音楽プレイヤーアプリ 要件定義書 v0.1

作成日: 2026-08-01 / ステータス: ドラフト（未決事項あり）

---

## 1. 背景と目的

### 背景
PCのiTunesにmp3を取り込んだが、iPhoneがiTunesに認識されず同期できなかった。結果として、手持ちのmp3をiPhoneで聴く手段がない状態。

### 目的
iTunesに依存せず、自分のmp3をiPhoneに取り込んで再生できる仕組みを、**完全無料・個人運用**で用意する。

### ゴール（成功の定義）
- 自分のmp3をiPhone上のアプリに取り込める
- オフライン（機内モード）で再生できる
- 1曲リピート / リストリピート / シャッフルが使える
- 自分でプレイリストを作って管理できる

---

## 2. 前提条件・制約

| 項目 | 内容 |
|---|---|
| 対象端末 | iPhone（iOS）1台のみ。他人への配布なし |
| 再生形態 | 端末内にファイルを保存してオフライン再生 |
| コスト | 完全無料。年額・月額の発生を一切許容しない |
| 開発体制 | 依頼者はコードを書かない。実装はすべて代行 |
| 運用 | 個人利用のみ。認証・マルチユーザーは不要 |
| 音源形式 | mp3（他形式は将来検討） |
| 曲数 | **未定**（要実測。初期目標を100曲/約500MBと仮置き） |

### コスト制約が方式に与える影響（重要）
iPhoneに「普通のアプリ」として入れるには、以下いずれかが必要:

- App Store配信 → Apple Developer Program **年99USD**（有料のため不可）
- Xcodeの無料プロビジョニング → **Macが必須** かつ **7日ごとに再インストール**（実質運用不可）

したがって **ネイティブiOSアプリという選択肢は制約により除外**される。

---

## 3. 方式選定

### 3.1 候補比較

| 方式 | 費用 | オフライン | 実現性 | 判定 |
|---|---|---|---|---|
| A. PWA（ホーム画面Webアプリ）+ 端末内保存 | 無料 | ◯ | 保存容量に不安 | **採用（要検証）** |
| B. PWA + 自宅PCから配信 | 無料 | ✗（自宅Wi-Fi内のみ） | ◯ | 代替案 |
| C. PWA + 無料クラウド配信 | 無料枠あり | △（キャッシュ次第） | ◯ | 代替案 |
| D. ネイティブiOSアプリ | 年99USD or Mac必須 | ◎ | 制約違反 | 除外 |
| E. 既存無料アプリ（VLC等）を使う | 無料 | ◎ | ◎ | 開発しない案 |

### 3.2 採用方式
**A. PWA（Progressive Web App）**

- HTML/CSS/JavaScriptで作ったWebアプリを、Safariの「ホーム画面に追加」で設置
- mp3ファイルはブラウザの **IndexedDB** に保存（端末内・オフライン再生）
- 見た目はアイコン起動・全画面で、ほぼ通常のアプリと同じ
- ホスティングはGitHub Pages等の無料枠（HTTPSがPWAの必須要件のため）

### 3.3 技術リスク（未確定・実機検証が必須）

以下は「動く可能性が高いが保証できない」項目。**本開発の前に検証すること。**

| # | リスク | 内容 | 影響 |
|---|---|---|---|
| R1 | **保存容量の上限** | iOSのWebアプリはストレージ割り当てが厳しく、数GB規模の保存は不安定という報告が多い | 曲数の上限が決まる。最悪、数百MB程度が限界 |
| R2 | **データの自動削除** | iOSは一定期間使われないWebアプリのデータを消す挙動がある（ホーム画面追加で緩和されるとされるが公式な明文化が乏しい） | 取り込んだ曲が消える可能性 |
| R3 | **ロック画面での再生継続** | Safariのタブでは継続するが、ホーム画面PWAでは停止する報告と、Media Session APIで動作する報告が混在。一時停止後30秒で操作不能になる不具合報告もある | 「ロック中も再生」が満たせない可能性 |
| R4 | ID3タグ読み取り | ライブラリ次第で動作差あり | 曲名表示がファイル名頼りになる |

### 3.4 リスクへの対応方針
- **PoC（技術検証）を最優先で実施**し、R1〜R3を実機で確認してから本開発に入る
- R1/R2対策として、**取り込み済み曲の一括バックアップ／再取り込み機能**を標準搭載する
- R3が不成立の場合の代替: ①Safariタブでの再生を正式運用にする ②方式Eへ切り替える

---

## 4. 機能要件

優先度: **MUST**=必須 / **SHOULD**=あると良い / **COULD**=将来

### 4.1 楽曲の取り込み

| ID | 機能 | 優先度 |
|---|---|---|
| F-01 | ファイル選択ダイアログからmp3を複数まとめて取り込む | MUST |
| F-02 | 取り込み時に端末内（IndexedDB）へ保存し、以降オフラインで再生可能にする | MUST |
| F-03 | 取り込み進捗の表示（n件中m件） | SHOULD |
| F-04 | 重複ファイルの検出とスキップ | SHOULD |
| F-05 | ID3タグから曲名・アーティスト・アルバムを自動取得（失敗時はファイル名を曲名にする） | COULD |

> **補足（運用フロー）**: PC上のmp3は、まずiPhoneから見える場所に置く必要がある。無料かつ最も簡単なのは **iCloud Drive**（PC側にiCloud for Windowsを入れてフォルダに放り込む）。以降、iPhoneの「ファイル」アプリ経由でアプリに取り込む。この経路自体も要件書確定後に手順化する。

### 4.2 ライブラリ

| ID | 機能 | 優先度 |
|---|---|---|
| F-10 | 全曲一覧の表示（曲名・アーティスト・再生時間） | MUST |
| F-11 | **検索**（曲名・アーティストの部分一致、インクリメンタル） | MUST |
| F-12 | **並び替え**（曲名順 / アーティスト順 / 追加日順） | MUST |
| F-13 | 曲の削除（端末内データも削除） | MUST |
| F-14 | 曲情報の手動編集（曲名・アーティスト） | SHOULD |
| F-15 | アルバムアートの表示 | COULD |

### 4.3 再生

| ID | 機能 | 優先度 |
|---|---|---|
| F-20 | 再生 / 一時停止 | MUST |
| F-21 | 次の曲 / 前の曲 | MUST |
| F-22 | シークバーによる再生位置の変更 | MUST |
| F-23 | 経過時間 / 総時間の表示 | MUST |
| F-24 | **ロック画面・コントロールセンターからの操作**（Media Session API） | MUST（※R3依存） |
| F-25 | **バックグラウンド再生の継続** | MUST（※R3依存） |
| F-26 | アプリ再起動時に前回の曲・再生位置を復元 | SHOULD |
| F-27 | 音量調整（端末側で代替可のため優先度低） | COULD |
| F-28 | 再生速度変更 / スリープタイマー | COULD |

### 4.4 再生モード

| ID | 機能 | 優先度 |
|---|---|---|
| F-30 | **リピートなし**（リスト末尾で停止） | MUST |
| F-31 | **1曲リピート** | MUST |
| F-32 | **リストリピート**（末尾の次は先頭へ） | MUST |
| F-33 | **シャッフル**（ON/OFFはリピート設定と独立して切替可能） | MUST |
| F-34 | 設定はアプリ再起動後も保持される | MUST |
| F-35 | シャッフル時、同じ曲が一巡するまで再登場しない方式とする | SHOULD |

### 4.5 プレイリスト

| ID | 機能 | 優先度 |
|---|---|---|
| F-40 | プレイリストの作成・名称変更・削除 | MUST |
| F-41 | ライブラリからプレイリストへ曲を追加 | MUST |
| F-42 | プレイリストから曲を除外（ライブラリの実体は残す） | MUST |
| F-43 | プレイリスト単位での再生（リピート/シャッフルが適用される） | MUST |
| F-44 | プレイリスト内の曲順の並び替え（ドラッグ操作） | SHOULD |
| F-45 | 1曲が複数のプレイリストに所属可能 | MUST |

### 4.6 管理・バックアップ

| ID | 機能 | 優先度 |
|---|---|---|
| F-50 | 現在の使用容量・曲数の表示 | MUST（R1把握のため） |
| F-51 | プレイリスト構成のエクスポート（JSON） / インポート | SHOULD（R2対策） |
| F-52 | 全データの初期化 | SHOULD |

---

## 5. 非機能要件

| 分類 | 要件 |
|---|---|
| コスト | 恒久的に0円。有料プラン・従量課金は使用しない |
| オフライン | 機内モードでもアプリ起動〜再生まで完結すること |
| 対応環境 | iOS Safari（最新版）。他ブラウザ・Androidは対象外だが動けば可 |
| 起動速度 | ホーム画面アイコンから3秒以内に操作可能な状態になること |
| 容量 | まずは500MB（約100曲）での安定動作を確認し、上限を実測で見極める |
| セキュリティ | 個人利用のためログイン不要。データは端末内のみ、外部送信なし |
| デザイン | **ダーク基調（黒〜濃いグレー）のかっこいい系**。片手・親指操作を前提としたレイアウト |
| 保守性 | 依頼者がコードを触らない前提。更新時はURLを開き直すだけで反映されること |

---

## 6. 画面構成

| # | 画面 | 内容 |
|---|---|---|
| S-1 | ライブラリ | 全曲一覧 / 検索バー / 並び替え / 曲の追加ボタン |
| S-2 | プレイリスト一覧 | 作成したプレイリストの一覧・新規作成 |
| S-3 | プレイリスト詳細 | 所属曲の一覧・並び替え・除外・まとめて再生 |
| S-4 | Now Playing（再生画面） | 大きなアートワーク領域 / シークバー / 再生操作 / リピート・シャッフル切替 |
| S-5 | ミニプレイヤー | 画面下部に常駐。タップでS-4へ展開 |
| S-6 | 設定 | 使用容量表示 / バックアップ / 初期化 |

ナビゲーション: 画面下部タブ（ライブラリ / プレイリスト / 設定）+ その上にミニプレイヤー常駐。

---

## 7. データモデル（IndexedDB）

```
tracks
  id          : string (UUID)
  title       : string
  artist      : string
  album       : string
  duration    : number (秒)
  fileName    : string
  fileSize    : number
  blob        : Blob        // mp3本体
  addedAt     : number

playlists
  id          : string (UUID)
  name        : string
  trackIds    : string[]    // 順序を保持
  createdAt   : number
  updatedAt   : number

settings
  repeatMode      : 'off' | 'one' | 'all'
  shuffle         : boolean
  lastTrackId     : string | null
  lastPositionSec : number
  sortOrder       : 'title' | 'artist' | 'addedAt'
```

---

## 8. 技術スタック（案）

| 領域 | 採用 | 理由 |
|---|---|---|
| 実装 | HTML + CSS + JavaScript（React等は使わない単一構成も検討） | 依頼者が保守しないため、軽量・依存最小を優先 |
| 保存 | IndexedDB（`idb`ライブラリ） | Blob保存に対応、容量が比較的大きい |
| 再生 | HTML `<audio>` + Media Session API | ロック画面操作に必要 |
| オフライン化 | Service Worker + Web App Manifest | ホーム画面追加・オフライン起動に必須 |
| ホスティング | GitHub Pages（無料・HTTPS標準） | PWAはHTTPS必須。恒久無料 |

---

## 9. スコープ外

- 他人への配布、App Store公開
- 複数端末間の同期
- ストリーミングサービス連携、歌詞表示、イコライザ
- mp3以外の形式（m4a/FLAC等）※将来検討
- Android / PCブラウザでの動作保証

---

## 10. 開発ステップ

| Phase | 内容 | 完了条件 |
|---|---|---|
| **0. PoC（最優先）** | 最小構成のPWAを作り実機検証 | R1（容量）R2（保持）R3（ロック画面再生）の可否が判明 |
| 1. 基盤 | 取り込み・保存・一覧・単純再生 | mp3を入れてオフライン再生できる |
| 2. 再生機能 | リピート3種・シャッフル・ロック画面操作 | 全MUST再生機能が動作 |
| 3. プレイリスト | 作成・追加・除外・並び替え | プレイリスト単位で再生できる |
| 4. 仕上げ | ダークUIの作り込み・検索/並び替え・容量表示・バックアップ | 常用に耐える状態 |

> Phase 0の結果次第で方式Aを取り下げ、方式B/C/Eへ切り替える判断を行う。

---

## 11. 未決事項（次回の確認事項）

| # | 項目 | 補足 |
|---|---|---|
| Q1 | 手持ちmp3の総容量 | PCのフォルダのプロパティで確認。R1の判断に直結する |
| Q2 | PCのOS（Windows / Mac） | iPhoneへのファイル受け渡し手順が変わる |
| Q3 | iCloud Driveの空き容量 | 無料枠5GB。取り込み経路として使えるかの判断 |
| Q4 | iPhoneのiOSバージョン | PWAの挙動がバージョン依存のため |
| Q5 | 「ロック画面再生」が技術的に不可だった場合の許容度 | 妥協して進めるか、方式を変えるか |
| Q6 | GitHubアカウントの有無 | ホスティングに使用（無料・新規作成可） |
